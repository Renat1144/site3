# Передача контекста проекта Turkey Signature

Этот файл хранит актуальное состояние проекта для продолжения работы в новой сессии или на другом устройстве. Смысловую часть обновляет Codex после каждой задачи, изменяющей проект. Кнопка **«Закончить работу»** дописывает внизу безопасный технический снимок.

Не добавлять сюда пароли, токены, содержимое `.env`, `wp-config.php`, дампы базы и персональные данные.

## Текущее состояние

Обновлено: 1 августа 2026 года.

### Что уже сделано

- В `wordpress_site3` настроен локальный WordPress в Docker на `http://localhost:8082/`; админка доступна по `http://localhost:8082/wp-admin/`.
- Создан премиальный одностраничный сайт авторских путешествий по Турции на теме `Turkey Signature`.
- Главная страница хранится как настоящая страница WordPress **«Главная»** и визуально редактируется блоками Gutenberg; общие шапка и подвал также находятся в редактируемой структуре страницы.
- Для новых страниц предусмотрена стартовая структура в стиле проекта без полной блокировки блоков.
- Статическая версия для GitHub Pages формируется из актуальной публичной страницы WordPress и сохраняется в корневой `index.html`.
- GitHub Pages опубликован по адресу `https://renat1144.github.io/site3/`.
- Приватный перенос между устройствами обновлён до формата версии 2: один ZIP содержит базу, uploads, локальные настройки, файлы проекта и все рабочие скрипты. Импорт архивов версии 1 сохранён.
- Добавлена система передачи контекста через этот файл и автоматический технический журнал при завершении работы.

### Последняя выполненная задача

- На Windows точно сопоставлены Docker-проекты: сайт `8080` использует `wordpress_wordpress_db`, а `wordpress_site3` на `8082` использует `wordpress_site3_db_data`.
- Только старый том `wordpress_site3_db_data` заменён новым томом Compose-проекта `wordpress_site3`; проект, контейнеры, сеть, файлы и том сайта `8080` не изменялись.
- База, uploads и локальное состояние `wordpress_site3` восстановлены из проверенного архива версии 2 `wordpress_site3-transfer-01-08-2026_00-09.zip`.
- В PowerShell- и Bash-импорте исправлены вызовы WP-CLI: перед `search-replace`, `eval-file` и `rewrite` теперь явно вызывается команда `wp`.
- Windows-импорт теперь извлекает имена ZIP как UTF-8 вручную с проверкой безопасных путей, чтобы macOS-архивы не искажали кириллические имена кнопок **«Начать работу»** / **«Закончить работу»**.
- Добавлен `.gitattributes`: Bash- и macOS-файлы закреплены за LF, PowerShell- и Windows CMD-файлы — за CRLF, чтобы перенос между платформами не ломал выполнение скриптов.

### Важные решения и ограничения

- WordPress является главным редактируемым источником; статический `index.html` — снимок для GitHub Pages.
- Публичная страница и редактор Gutenberg должны оставаться единой реализацией и визуально совпадать.
- В GitHub нельзя добавлять закрытые архивы, `.env`, `wp-config.php`, локальные логины и дампы базы.
- Форма заявки пока остаётся обозначенным местом-заполнителем: адресат заявок и окончательный текст согласия не предоставлены.
- Сырой снимок заменённого тома сохранён в `backups/volume-snapshots/`; страховочный архив свежего состояния перед импортом сохранён в `backups/`. Оба каталога закрытые и не должны попадать в Git.
- Четыре дублирующих файла с искажёнными кириллическими именами, созданные старой распаковкой, сохранены для анализа в `backups/import-name-artifacts-20260801-0034/` и удалены из рабочего корня.

### Проверено

- Реальное создание приватного архива версии 2 через Docker.
- Контрольная сумма SHA-256 и проверка структуры ZIP.
- Наличие и точное совпадение рабочих скриптов, `PROJECT_HANDOFF.md` и кнопок завершения внутри архива.
- Наличие базы, `local.env`, `wp-config.php` и локальных данных входа в том же едином ZIP.
- Проверка архивов версий 1 и 2 в безопасном режиме без изменения локальной базы.
- Работа macOS handoff-скрипта: техническая запись добавляется без секретов, контейнеры определяются как запущенные.
- Синтаксис изменённых Bash-скриптов и macOS-кнопки завершения, а также отсутствие ошибок форматирования Git diff.
- Проверено точное подключение томов и сетей обоих Docker-проектов до и после замены; сайт `8080` всё время возвращал HTTP 200.
- Новый `wordpress_site3_db_data` здоров, используется только контейнером `wordpress_site3-db-1`; сайт и админка на `8082` возвращают HTTP 200.
- Активна тема `turkey-signature`; главная страница **«Главная»** опубликована с ID 6; восстановлено 12 файлов uploads, на публичной странице нет битых изображений.
- Вход в WordPress и открытие страницы **«Главная»** в Gutenberg проверены. Список блоков содержит шапку, основную группу и подвал; автоматизированный браузер не отрисовал содержимое blob-iframe холста, поэтому визуальное совпадение холста редактора требует повторной ручной или браузерной проверки.
- Синтаксис исправленного PowerShell-импорта и Bash-импорта проверен; архив версии 2 повторно прошёл безопасную проверку без изменения данных.
- Все отслеживаемые `.sh` и `.command` файлы проверены через `bash -n` внутри Linux-контейнера.

### С чего продолжать

- Перед следующими изменениями повторно проверить визуальную отрисовку холста Gutenberg для страницы **«Главная»**; данные блоков и публичная версия уже подтверждены.
- Исправить статическую публикацию пользовательских медиа: текущий GitHub Pages `index.html` ссылается на один файл из игнорируемого `wp-content/uploads`, который на GitHub возвращает 404.
- Для следующих публикаций снова требуется прямое разрешение пользователя на commit и push.

## Автоматический журнал завершения работы

Записи ниже добавляются скриптом. Они содержат только дату, платформу, состояние Git и Docker без секретов и содержимого пользовательских файлов.

### Завершение работы: 2026-07-31 23:59:51 MSK

- Платформа: `Darwin`
- Ветка Git: `main`
- Commit: `785d3fa`
- Изменённые и новые файлы:

```text
 M AGENTS.md
 M LOCAL_SETUP.md
 M README.md
 M script-local-export.ps1
 M script-local-export.sh
 M script-local-import.ps1
 M script-local-import.sh
 M "Закончить работу.cmd"
 M "Закончить работу.command"
?? PROJECT_HANDOFF.md
?? script-project-handoff.ps1
?? script-project-handoff.sh
```

- Состояние Docker Compose:

```text
db: running
wordpress: running
```

### Завершение работы: 2026-08-01 00:09:52 MSK

- Платформа: `Darwin`
- Ветка Git: `main`
- Commit: `2d701e6`
- Изменённые и новые файлы: нет.

- Состояние Docker Compose:

```text
db: running
wordpress: running
```
